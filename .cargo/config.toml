# The official crates.io endpoint is unreliable on the primary development network.
# rsproxy mirrors the crates.io sparse index and crate archives without changing checksums.
[source.crates-io]
replace-with = "rsproxy-sparse"

[source.rsproxy-sparse]
registry = "sparse+https://rsproxy.cn/index/"

[net]
retry = 3
